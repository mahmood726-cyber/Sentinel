---
name: P0-claude-config-committed
description: 'Agent tool configuration directories (.claude/, .gemini/, .codex/) must never be committed to public repos per CLAUDE.md#config-safety.'
trigger: commit
severity: BLOCK
scope: repo
source: 'CLAUDE.md#config-safety'
---

Agent tool configuration directories (.claude/, .gemini/, .codex/) must never be committed to public repos per CLAUDE.md#config-safety.

---

_Auto-exported from `sentinel/rules/yaml/P0-claude-config-committed.yaml` by `scripts/export_skills.py`. Source kind: yaml. Manifest-hashed in `rules-manifest.json`. Do not edit this file directly; edit the rule and re-run the exporter._
