---
name: P1-empty-dataframe-access
description: 'Positional DataFrame/Series access (`.iloc[0]`, `.iloc[-1]`, `.values[0]`) raises IndexError on empty inputs. Named in MEMORY.md#top-5-cross-project -defects as one of the five repeat offenders (alongside XSS, hardcoded paths, localStorage collisi...'
trigger: commit
severity: WARN
scope: repo
source: 'MEMORY.md#top-5-cross-project-defects'
---

Positional DataFrame/Series access (`.iloc[0]`, `.iloc[-1]`, `.values[0]`) raises IndexError on empty inputs. Named in MEMORY.md#top-5-cross-project -defects as one of the five repeat offenders (alongside XSS, hardcoded paths, localStorage collision, SE_FLOOR). Listed in lessons.md#common- regression-traps as "empty datasets" under high-friction targets.

---

_Auto-exported from `sentinel/rules/yaml/P1-empty-dataframe-access.yaml` by `scripts/export_skills.py`. Source kind: yaml. Manifest-hashed in `rules-manifest.json`. Do not edit this file directly; edit the rule and re-run the exporter._
